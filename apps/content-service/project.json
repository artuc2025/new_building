{
  "name": "content-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/content-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @new-building-portal/content-service build",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "migration:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/migration-cli/generate.ts --name {args.name}",
        "cwd": "{projectRoot}"
      }
    },
    "migration:run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/migration-cli/run.ts",
        "cwd": "{projectRoot}"
      }
    },
    "migration:revert": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/migration-cli/revert.ts",
        "cwd": "{projectRoot}"
      }
    },
    "migration:diagnose": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/migration-cli/diagnose.ts",
        "cwd": "{projectRoot}"
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/migration-cli/run.ts",
        "cwd": "{projectRoot}"
      }
    }
  },
  "tags": []
}

